{
  "name": "drawing-project",
  "version": "1.0.0",
  "description": "Collaborative Drawing App",
  "scripts": {
    "postinstall": "cd collaborative-canvas/client && npm install && cd ../server && npm install",
    "build": "cd collaborative-canvas/client && npm run build && cd ../server && npm run build",
    "start": "cd collaborative-canvas/server && npm start"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}